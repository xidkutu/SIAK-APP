<%@ page import="pendaftaran.*,ulang.*"%>

<%
/*    Login supervisorHome = (Login) session.getAttribute("remoteSupv");
    ulang.OtorisasiCetakUlang supv = (ulang.OtorisasiCetakUlang) session.getAttribute("remoteOto");
    String authKey = (String) session.getAttribute("authKey4CetakUlang");
    System.out.println("authKey="+authKey);
    boolean authorized = false;

      if (supervisorHome!=null) {
        System.out.println("supervisorHome ok");
        UserInfo1 supervisor = supervisorHome.getUserInfo();
        System.out.println("cek");
        if ((record!=null) && (supervisor!=null) && (supv != null) && (authKey != null)) {
          System.out.println("cek otorisasi");
          authorized = supv.cekOtorisasi(record, supervisor, authKey);
          System.out.println("otorisasi ok");
        }
   }
*/
%>